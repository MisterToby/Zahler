{% extends '::base.html.twig' %}

{% block body -%}
    <h1>AccountType</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Actname</th>
                <td>{{ entity.actName }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('accounttype') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('accounttype_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
